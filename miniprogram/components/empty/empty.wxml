<view class="empty">
  <view class="emoji">{{icon}}</view>
  <view class="title">{{title}}</view>
  <view class="desc" wx:if="{{desc}}">{{desc}}</view>
  <button wx:if="{{btnText}}" class="empty-btn" bindtap="onBtnTap">{{btnText}}</button>
</view>
